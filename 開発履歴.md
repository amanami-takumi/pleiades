## 2026-06-22

- 投資情報チャートに100日線と200日線の移動平均線を個別にオーバーレイ表示できるオプションを追加。
- 投資情報チャートで通常操作中に2点クリックした範囲の騰落率を表示できるよう追加。

## 2026-07-07

- 外部連携の日足APIで、保存済み価格履歴から計算した MA20/MA60/MA200、MACDヒストグラム、ボリンジャーバンド上下限2σ、RSI(14) を返すよう拡張。
- 日付範囲指定時も指標が範囲内データだけで歪まないよう、銘柄の全保存履歴で計算してからレスポンス範囲へ付与する方式にした。

## 2026-07-05

- 家庭内の別サービス向けに、保存済み投資情報を読み取り専用で提供する外部連携APIを追加。
- 登録銘柄一覧と、ティッカー指定・日付範囲指定による日足 OHLCV 取得APIを追加。
- `EXTERNAL_API_KEY` を設定した場合、外部連携APIで `X-Pleiades-Api-Key` ヘッダーを要求するよう実装。

## 2026-07-04

- 家計簿システムを新しいタブとして追加。
- SQLite に家計簿取引用テーブルを追加し、サンプルの銀行取引明細・デビットカード明細を Shift-JIS 系CSVとして読み込む取り込みAPIを追加。
- デビットカード明細を支出明細として取り込み、銀行明細の `VISAデビット` 行は二重計上を避けるためサンプル一括取り込み時に除外するよう実装。
- 店名・摘要から食費、日用品、娯楽、サブスク、交通、通信、住居、投資・貯蓄などの初期カテゴリを推定する処理を追加。
- 家計簿タブに月選択、サンプル取り込み、収入・支出・収支、カテゴリ別支出、月次推移、明細一覧、カテゴリ修正、集計除外操作を追加。
- Docker Compose のバックエンドへサンプルCSVを読み取り専用でマウントし、コンテナ上でもサンプル取り込みできるよう変更。
- 家計簿システムに手動CSVアップロード機能を追加し、銀行取引明細・デビットカード明細の同時アップロードを自動判定して取り込めるよう変更。
- デビットカード明細取り込み時、対応する銀行明細の `VISAデビット` 引き落としを集計除外して二重計上を避ける処理を追加。
- 月次推移の表示順を新しい月から古い月へ変更。
- 銀行明細の取引後残高から資産推移を集計し、家計簿タブにグラフ表示する機能を追加。

## 2026-06-01

- Pleiades の初期実装を作成。
- Docker Compose による FastAPI バックエンドと Vue/Tailwind フロントエンドを追加。
- SQLite 永続化、yfinance 取得、ウォッチリスト、チャート、主要指標表示を実装。
- タスク管理とスマートホームは後日実装タブとして追加。

## 2026-06-02

- フロントエンドの API 呼び出しから `localhost` の固定値を削除。
- Vite dev server の `/api` proxy で backend コンテナへ転送する構成に変更。
- VPN 内の IP アドレスから `http://100.100.27.52:3050` でアクセスしても API に到達できるよう修正。
- yfinance の取得失敗を銘柄ごとに `last_error` として SQLite に保存し、ウォッチリストと詳細画面に表示するよう修正。
- 価格履歴が空の銘柄を成功扱いにせず、更新 API の `errors` に理由を返すよう変更。
- yfinance/Yahoo Finance 側のレート制限対策として、リクエスト間隔、銘柄間隔、レート制限時のバックオフ再試行を追加。
- 投資情報チャートに縦軸の価格ラベルと横軸の日付ラベルを追加。
- 非上場投資信託向けに Yahoo!ファイナンス時系列 HTML のフォールバック取得を追加。
- 投信協会コード形式の `asset_type=fund` は yfinance を先に叩かず、Yahoo!ファイナンス HTML から基準価額を保存するよう変更。
- Yahoo!ファイナンスの投信時系列 BFF API を利用し、投信協会コードの5年分の日次基準価額を取得できるよう拡張。
- BFF API が利用できない場合は従来の HTML 表スクレイピングへフォールバックする構成に変更。
- 更新処理を同期実行からキュー方式に変更し、更新ジョブの一覧、状態、エラーを閲覧できる API/UI を追加。
- 全銘柄更新に加えて、選択中の銘柄だけを更新キューへ投入できる機能を追加。
- 更新ジョブの中止 API/UI を追加。待機中ジョブは即時中止、実行中ジョブは中止要求後に現在の取得完了時点で中止扱いにする方式とした。
- 銘柄詳細に最終更新日時を表示。
- 購入履歴テーブルと API/UI を追加し、複数回購入の投資額、保有数量、評価額、評価損益を表示。
- チャートにマウスオーバー時の日付・価格ツールチップを追加。
- 個別株の購入履歴入力では「一株価格」と数量から購入総額を算出して保存するよう変更。
- 銘柄にタグと表示順を保存するよう変更し、ウォッチリストをタグタブで切り替えられるよう追加。
- ウォッチリストの銘柄をドラッグ&ドロップで並び替え、DBに保存できるよう追加。
- 更新キューを折りたたみ可能に変更。

## 2026-06-03

- チャートにライン表示と日線表示を切り替えるオプションを追加。
- チャートに5日線、25日線、75日線の移動平均線を個別にオーバーレイ表示できるオプションを追加。
- チャート上のドラッグ操作でサポートラインとレジスタンスラインを描画できるオプションを追加。
- 最終更新日時、最終試行、更新キューの時刻表示を UTC から日本時間表示に変換。
- 定時・随時の市場データ取得から yfinance 依存を削除し、Yahoo Finance のチャート/quote レスポンスを直接取得して株式・指数を保存する方式に変更。
- 投資信託の購入履歴は購入日と購入額だけで登録し、購入日の基準価額から評価用数量を自動計算するよう変更。
- Yahoo Finance の銘柄ページ HTML から PER と配当利回りをスクレイピングし、quote API で欠損する場合も主要指標を保存できるよう変更。
- Yahoo Finance の銘柄ページ HTML から銘柄名を取得して保存し、画面では銘柄名を主表示、ティッカーを副表示に変更。

## 2026-06-08

- 自動の一斉更新を日本時間23時台に1日1回だけキュー投入する方式へ変更。
- 更新キューのジョブ処理間に待機時間を入れ、連続ジョブの外部リクエスト開始間隔を空けるよう変更。
- チャートの期間、ライン/日線、移動平均線、支持/抵抗線の操作群をチャート直上に移動し、現在値や騰落率などの指標と配置を入れ替え。
- Yahoo Finance の quote/API と銘柄ページ HTML から PBR と ROE を取得し、日本株の PBR/ROE も保存できるよう修正。
- 履歴 API で利用する `timedelta` の import 漏れにより Internal Server Error になる不具合を修正。
- 銘柄詳細ヘッダーで長い銘柄名が表示しやすいよう、現在値・騰落率などの指標をヘッダー直下の別行に配置。

## 2026-06-09

- タスク管理システムを実装。
- SQLite にタスク、タグ、タスクタグ関連テーブルを追加。
- FastAPI にタスクとタグの作成・一覧・更新・削除 API を追加。
- フロントエンドにタスク管理タブを追加し、名前、進行度、期限、継続期間、タグ、詳細を登録・編集できるよう実装。
- タスク一覧で名前・期限・タグによるソート、タグの色設定、タグ単位の非表示、完了タスクの時系列表示を追加。
- タスク一覧を表形式へ変更し、名前、進行度、期限、継続期間、タグ、詳細の列ヘッダーから昇順・降順でソートできるよう改善。

## 2026-06-15

- 投資支援システムを新しいタブとして追加。
- `売買ルール.csv` のルールを読み込み、保存済み価格履歴から現在シグナルを抽出する API/UI を実装。
- ルール別に上昇・下降の正答率、期待騰落率、平均騰落幅を集計するバックテスト API/UI を追加。
- 現在シグナルから投資情報タブの該当銘柄へ移動できるようにした。
- 現在シグナルをルール順・銘柄順・区分順で並び替えられるようにした。
- バックテストをバックエンドの非同期再計算方式に変更し、過去5年を検証対象に戻した。
- 投資情報の更新後に現在シグナルとバックテストを日本時間25時に自動再計算するよう追加。
- 投資支援タブの再計算操作を「バックテスト再試行」として明示した。
- 現在シグナルの並び替え操作をドロップダウンリストに変更。
- 売買ルール分析に曜日別の全体日次リターン、シグナル発生日リターン、1/3/5営業日後リターン、signal × weekday の交互作用差分を追加。
- 現在シグナルのドロップダウンを、ルール別・区分別・銘柄別の表示対象選択に変更。
- 現在シグナルの銘柄表示に RSI(14) と RSI(2) の数値を追加。
- 曜日分析にメジャーSQ週の市場日次リターン、シグナル数、1/3/5営業日後リターン、交互作用差分を追加。
- `売買ルール.csv` 16行目以降の BB/MACD 系ルールを投資支援システムのシグナル判定・バックテストに反映。
- BB/MACD 系ルールの判定高速化のため、銘柄ごとにボリンジャーバンド、MACD、出来高平均、移動平均を事前計算する方式に変更。
- ルール別バックテストの現在シグナル数から該当ルールの現在シグナル表示へ絞り込めるように変更。
- 現在シグナルのカードクリックを銘柄別絞り込みに変更し、投資情報への遷移はティッカークリックに限定。
- 曜日分析をルール別バックテスト行の開閉詳細に統合。
- 現在シグナルの買い/売りバランスとルール別シグナル強度を表示。
- シグナル強度と買い/売りバランスを銘柄ごとに表示するよう変更。
- シグナル強度を、値幅抽出系ルールは平均騰落幅、方向抽出系ルールは期待騰落率を元に買い/売り別で標準化する方式へ変更。

## 2026-06-16

- 投資支援システムのルール定義を `quant_rules_categorized.csv` 優先で読み込むよう変更し、ルールに主カテゴリとカテゴリタグを付与。
- ルール別バックテストに加えてカテゴリ別バックテストを追加し、カテゴリ別にも曜日分析を開閉表示できるよう実装。
- `category_interaction_matrix.csv` のカテゴリ間関係を数値化し、現在シグナルの銘柄別買い/売り強度へ反映。
- バックテストの検証年数と判定営業日を画面から指定して再計算できるよう、API/UI を拡張。

## 2026-06-17

- カテゴリ別バックテストを `category_interaction_matrix.csv` の10×10カテゴリ関係検証へ変更し、カテゴリA単独平均との差分とマトリクス上の関係を表示するよう修正。
- 稼働中バックエンドコンテナを再作成し、`quant_rules_categorized.csv` と `category_interaction_matrix.csv` が実行環境へマウントされる状態に更新。
- カテゴリ別バックテストとルール別バックテストの列ヘッダーから現在件数、期待騰落率、正答率などで並び替えられるよう変更。
- カテゴリ別バックテストの現在件数から該当銘柄を展開表示し、銘柄クリックで現在シグナルを該当銘柄に絞り込めるよう追加。
- カテゴリ別バックテストとルール別バックテストをセクション単位で折りたためるよう変更。
